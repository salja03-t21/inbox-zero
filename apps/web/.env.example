DATABASE_URL="postgresql://postgres:password@localhost:5432/inboxzero?schema=public"
DIRECT_URL="postgresql://postgres:password@localhost:5432/inboxzero?schema=public"
NEXT_PUBLIC_BASE_URL=http://localhost:3000

AUTH_SECRET= # Generate a random secret here: https://generate-secret.vercel.app/32

# Gmail
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_ENCRYPT_SECRET= # openssl rand -hex 32
GOOGLE_ENCRYPT_SALT= # openssl rand -hex 16

GOOGLE_PUBSUB_TOPIC_NAME="projects/abc/topics/xyz"
GOOGLE_PUBSUB_VERIFICATION_TOKEN= # Generate a random secret here: https://generate-secret.vercel.app/32

# Outlook
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_ENCRYPT_SECRET= # openssl rand -hex 32
MICROSOFT_ENCRYPT_SALT= # openssl rand -hex 16
MICROSOFT_WEBHOOK_CLIENT_STATE=

# LLM config
DEFAULT_LLM_PROVIDER=anthropic
DEFAULT_LLM_MODEL=

# Set at least one of the following:
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=
# OPENAI_API_KEY=
# OPENAI_BASE_URL= # Optional: Custom base URL for OpenAI-compatible providers (e.g., https://api.tokenfactory.nebius.com/v1/ for Nebius)
# GOOGLE_API_KEY=
# GROQ_API_KEY=
# BEDROCK_ACCESS_KEY=
# BEDROCK_SECRET_KEY=
# BEDROCK_REGION=us-west-2
# OLLAMA_BASE_URL=http://localhost:11434/api
# NEXT_PUBLIC_OLLAMA_MODEL=phi3

# Economy LLM configuration (for large context windows where cost efficiency matters)
ECONOMY_LLM_PROVIDER=
ECONOMY_LLM_MODEL=

INTERNAL_API_KEY= # Generate a random secret here: https://generate-secret.vercel.app/32
API_KEY_SALT= # Generate a random secret here: https://generate-secret.vercel.app/32

# ============================================================================
# ACCESS CONTROL & AUTHENTICATION CONFIGURATION
# ============================================================================
# These settings control who can access your application and which
# authentication methods are available on the login page.

# -----------------------------------------------------------------------------
# Domain-Based Access Control
# -----------------------------------------------------------------------------
# Restrict user sign-ups to specific email domains. Only users with email
# addresses from the specified domains will be able to create accounts.
#
# Format: Comma-separated list of allowed domains (without @ symbol)
# Example: ALLOWED_EMAIL_DOMAINS=company.com,partner.org,subsidiary.net
#
# Behavior:
# - When configured: Only specified domains can sign in
# - When empty: All email domains are allowed (default/open access)
# - Domain matching: Case-insensitive, exact domain match only
# - Works with: All authentication providers (Google, Microsoft, SSO)
#
# Use cases:
# - Enterprise: Restrict to company domain only
# - Multi-tenant: Allow multiple partner domains
# - Open: Leave empty for public access
ALLOWED_EMAIL_DOMAINS=

# -----------------------------------------------------------------------------
# Authentication Provider Control
# -----------------------------------------------------------------------------
# Control which sign-in methods appear on the login page. Each authentication
# method can be individually enabled or disabled.
#
# Options: true (enabled) or false (disabled)
# Default: true for all providers if not specified
#
# ENABLE_GOOGLE_AUTH
#   Shows/hides "Sign in with Google" button
#   Requires: GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET configured above
#
# ENABLE_MICROSOFT_AUTH
#   Shows/hides "Sign in with Microsoft" button
#   Requires: MICROSOFT_CLIENT_ID and MICROSOFT_CLIENT_SECRET configured above
#
# ENABLE_SSO_AUTH
#   Shows/hides "Sign in with SSO" link
#   Requires: SSO configuration (see better-auth SSO documentation)
#
# Configuration examples:
# - Google only:     ENABLE_GOOGLE_AUTH=true, others=false
# - Microsoft only:  ENABLE_MICROSOFT_AUTH=true, others=false
# - No SSO:          ENABLE_SSO_AUTH=false, others=true
# - All methods:     All set to true (default)
#
# Note: If all methods are disabled, users will see an error message.
# Changes require server restart to take effect.
ENABLE_GOOGLE_AUTH=true
ENABLE_MICROSOFT_AUTH=true
ENABLE_SSO_AUTH=true

#redis config
UPSTASH_REDIS_URL="http://localhost:8079"
UPSTASH_REDIS_TOKEN= # Generate a random secret here: https://generate-secret.vercel.app/32
REDIS_URL= # used for subscriptions: rediss://:password@host:port
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# Inngest (self-hosted) - Set these to use Inngest instead of QStash
INNGEST_EVENT_KEY= # openssl rand -hex 16
INNGEST_SIGNING_KEY= # openssl rand -hex 32
INNGEST_BASE_URL=http://inngest:8288

# Optional:

NEXT_PUBLIC_APP_HOME_PATH=/assistant # If you want the product to default to email client, set this to /mail
LOG_ZOD_ERRORS=true
CRON_SECRET=

# Tinybird
TINYBIRD_TOKEN=
TINYBIRD_BASE_URL=https://api.us-east.tinybird.co/
TINYBIRD_ENCRYPT_SECRET= # openssl rand -hex 32
TINYBIRD_ENCRYPT_SALT= # openssl rand -hex 16

# Sentry (error tracking)
SENTRY_AUTH_TOKEN=
SENTRY_ORGANIZATION=
SENTRY_PROJECT=
NEXT_PUBLIC_SENTRY_DSN=

# Axiom (logging)
NEXT_PUBLIC_AXIOM_DATASET=
NEXT_PUBLIC_AXIOM_TOKEN=

# PostHog (analytics)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HERO_AB=
NEXT_PUBLIC_POSTHOG_ONBOARDING_SURVEY_ID=
POSTHOG_API_SECRET=
POSTHOG_PROJECT_ID=

# Self-hosted: Set to "true" to disable Vercel Analytics (prevents console errors)
NEXT_PUBLIC_VERCEL_ANALYTICS_DISABLED=false

# Marketing emails
RESEND_API_KEY=
LOOPS_API_SECRET=

# Crisp support chat
NEXT_PUBLIC_CRISP_WEBSITE_ID=

# Sanity config for blog. (Not needed. Only for blog):
# NEXT_PUBLIC_SANITY_PROJECT_ID=
# NEXT_PUBLIC_SANITY_DATASET="production"
